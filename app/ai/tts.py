def synthesize_speech(text: str) -> bytes:
    """
    MOCK: real version will call a TTS engine (e.g. ElevenLabs, Azure TTS,
    or a local model) and return actual audio bytes (mp3/wav).
    """
    return b""  # placeholder empty audio